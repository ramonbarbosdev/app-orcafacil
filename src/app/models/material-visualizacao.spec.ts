import { MaterialVisualizacao } from './material-visualizacao';

describe('MaterialVisualizacao', () => {
  it('should create an instance', () => {
    expect(new MaterialVisualizacao()).toBeTruthy();
  });
});
