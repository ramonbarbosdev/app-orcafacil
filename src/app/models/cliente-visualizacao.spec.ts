import { ClienteVisualizacao } from './cliente-visualizacao';

describe('ClienteVisualizacao', () => {
  it('should create an instance', () => {
    expect(new ClienteVisualizacao()).toBeTruthy();
  });
});
