import { ConfiguracaoNotificacao } from './configuracao-notificacao';

describe('ConfiguracaoNotificacao', () => {
  it('should create an instance', () => {
    expect(new ConfiguracaoNotificacao()).toBeTruthy();
  });
});
